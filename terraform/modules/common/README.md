# Module: common

Provides shared data sources and locals used by all other modules.

## Outputs

| Name                 | Description                              |
|----------------------|------------------------------------------|
| `availability_domain`| First AD in the target region            |
| `common_tags`        | Standard tag map applied to all resources|
